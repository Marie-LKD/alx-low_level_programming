#include "main.h"
/**
*_strlen - function returns the kength of a string
*@s: string
*Return: integer length of a string
*/
int _strlen(char *s)
{
int length;
length = 0;
while (*s != '\0')
{
s++;
lenght++;
}
return (length);
}
